# Drag-and-drop fixtures

These files are development fixtures for manually testing the Animal Buddy drop target:

- `AnimalBuddyTestImage.svg` should show the camera and SD-card presentation.
- `AnimalBuddyTestText.txt` should show the question-mark presentation.
- `AnimalBuddyTestURL.webloc` should show the envelope and link presentation.
- `AnimalBuddyTestFolder` exercises directory detection.
